from setuptools import setup, find_packages

setup(name='BananaNet',
    version='5.0',
    description='Keras + Caffee training. mxnet predict. OpenCV Camera + Draw',
    author='Banana',
    install_requires=['mxnet', 'opencv-python', 'numpy']
)
